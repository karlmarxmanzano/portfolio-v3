# Karl Portfolio — Next.js

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Customize

- Put your portrait at `public/your-photo.jpg`.
- Replace placeholder email, LinkedIn, résumé, company, and project URLs in `app/page.tsx`.
- Global styling is in `app/globals.css`.
